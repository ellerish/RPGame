using System;
using System.Collections.Generic;
using System.Text;

namespace RPGame.Items.Armor
{
    //enum for Slots, assiged with value for scaling

    public enum Slots
    {
       Head = 80, //head has 80% scaling
       Body = 100, //body has 100% scaling
       Legs = 60, //legs has 60% scaling
    }
}
